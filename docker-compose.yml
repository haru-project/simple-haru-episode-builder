services:
  episode-builder:
    build: .
    environment:
      - PORT=${PORT:-8501}
    ports:
      - "${PORT:-8501}:${PORT:-8501}"
    volumes:
      - ./data/tasks/saved:/app/data/tasks/saved
      - ./data/tasks/templates:/app/data/tasks/templates
